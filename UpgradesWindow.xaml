<Window x:Class="BalatroWPF.UpgradesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose an Upgrade" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Horizontal, square tiles for choices -->
        <ItemsControl x:Name="ChoicesList">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Width="140" Height="140" Margin="6" CornerRadius="8" Background="#FFF4E1" BorderBrush="#FFB270" BorderThickness="1"
                            ToolTipService.Placement="Right">
                        <Border.ToolTip>
                            <ToolTip Placement="Right" StaysOpen="True">
                                <StackPanel Width="260">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Cost, StringFormat='Cost: {0} $'}" Margin="0,6,0,0" FontStyle="Italic" />
                                </StackPanel>
                            </ToolTip>
                        </Border.ToolTip>

                        <Grid>
                            <StackPanel Margin="8">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Cost, StringFormat='Cost: {0} $'}" Margin="0,6,0,0"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="8">
                                <Button Content="Buy" Tag="{Binding}" Width="70" Click="BuyButton_Click"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Skip" Width="80" Margin="6" Click="SkipButton_Click"/>
        </StackPanel>
    </Grid>
</Window>